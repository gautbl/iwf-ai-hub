version: 2

sources:
  - name: openweightlifting
    description: "Raw tables written by the Python extract_results.py loader (Bronze layer)."
    schema: main
    tables:
      - name: raw_ow_events
        description: "All OpenWeightlifting event results (unified table), created by extract_results.py."
